function LoginPage() {
  return (
    <div>
      <main>
        <button>Login with Google</button>
        <button>Login with email</button>
      </main>
    </div>
  );
}

export default LoginPage;
